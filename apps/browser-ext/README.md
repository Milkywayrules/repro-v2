# browser-ext

WXT + React MV3 extension. Reference wiring for `@repro-v2/api-client`, `@repro-v2/auth/react`, and platform probes.

Popup: session gate via Console login link, `GET /ready` dot, authenticated `GET /api/v1/task-lists` (read-only).
