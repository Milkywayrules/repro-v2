{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun run --filter api build"
  },
  "deploy": {
    "startCommand": "bun run --filter api start",
    "healthcheckPath": "/ready",
    "preDeployCommand": [
      "sh",
      "-c",
      "echo 'ACCEPTED: db:push is intentional pre-staging — switch to db:migrate after staging hardening' && bun run --filter @repro-v2/db db:push"
    ],
    "restartPolicyType": "ON_FAILURE"
  }
}
