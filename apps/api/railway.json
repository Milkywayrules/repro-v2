{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install --frozen-lockfile --filter api... && bun run --filter api build",
    "watchPatterns": ["apps/api/**", "packages/**", "package.json", "bun.lock"]
  },
  "deploy": {
    "startCommand": "bun run --filter api start",
    "healthcheckPath": "/ready",
    "preDeployCommand": [
      "sh -c echo 'ACCEPTED: db:push is intentional pre-staging — switch to db:migrate after staging hardening' && bun run --filter @repro-v2/db db:push"
    ],
    "restartPolicyType": "ON_FAILURE"
  }
}
