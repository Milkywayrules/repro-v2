{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun run --filter docs build"
  },
  "deploy": {
    "startCommand": "bun run --filter docs start",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
