# @repro-v2/api-schemas

Zod schemas for API request bodies, query params, and shared input validation.

Used by `apps/api` routes today; frontends when forms need shared validation. Contract *output* types live in `@repro-v2/api-types/contract`.

Monorepo type ownership: root [AGENTS.md](../../AGENTS.md).
